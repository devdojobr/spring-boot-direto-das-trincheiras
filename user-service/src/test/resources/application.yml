spring:
  sql:
    init:
      mode: never
  jpa:
#    defer-datasource-initialization: true
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration-test
  web:
    error:
      include-message: always
      include-binding-errors: always


brasil-api:
  base-url: http://localhost:${wiremock.server.port:8089}/api
  cep-uri: /cep/v1/{cep}